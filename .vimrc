" Most of the file copied from Addy's repo as I'm not a vim expert (I know how to exit it though!)"
set nocompatible
set esckeys
let mapleader=","
set backupdir=~/.vim/backups
set directory=~/.vim/swaps
if exists("&undodir")
  set undodir=~/.vim/undo
endif
syntax on
set tabstop=2
set hlsearch
set ignorecase
set incsearch
set nostartofline
set shortmess=atI
set showmode
set title
